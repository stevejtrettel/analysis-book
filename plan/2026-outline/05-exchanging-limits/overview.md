## Chapter 5: When Order Matters

### Overview

Finite sums commute, but infinite sums are different. Rearranging an infinite series can change its sum — or make it diverge. Swapping the order of two limits, or interchanging a limit with a sum, can produce different answers. This chapter maps out when these operations are safe. The recurring theme: absolute convergence and domination conditions tame the wildness of infinity.

### Sections

1. **§5.1 Rearrangements** — Absolute convergence is robust (any rearrangement gives the same sum), conditional convergence is fragile (Riemann: can rearrange to any value). Guided exercise: rearranging the alternating harmonic series.

2. **§5.2 Iterated Limits** — Double sequences, iterated limits that differ, the double limit and when it forces agreement, paths to infinity.

3. **§5.3 Double Sums** — When order matters (row sums ≠ column sums), when order doesn't (absolute convergence over the grid), multiplying series via the Cauchy product, Mertens' theorem. Applications: squaring the geometric series, the exponential functional equation.

4. **§5.4 Dominated Convergence** — Interchanging limits and infinite sums, the danger (example where they don't commute), dominated convergence for series. The big application: $(1 + x/n)^n = \sum x^k/k!$, connecting Chapter 2's $e$ to the series.

### Dependencies

**Requires:**
- §2.4 (series, $e = \lim(1+1/n)^n$)
- §3.3 (Cauchy criterion)
- §4.2 (absolute vs conditional convergence)

**Leads to:**
- Chapter 6 ($\exp(x) = \sum x^n/n!$ defined, functional equation proven, all properties derived)